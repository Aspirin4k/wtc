# Frontend

```
// Установить зависимости
npm i

// Запустить dev режим. Может подняться не с первого раза, но будьте настойчивы
// localhost:3001
npm run start-dev

// Собрать prod бандл
npm run build

// Запустить prod сборку
npm run start
```