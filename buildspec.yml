version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
      nodejs: 14
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn install -P api
      - cd frontend && npm i && npm run build && cd ..
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - target/wtc-backend-1.0.jar
    - frontend/dist/**/*
    - appspec.yml
    - post_deploy.sh
    - swap_release.sh